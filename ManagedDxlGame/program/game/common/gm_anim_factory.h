#pragma once
#include "gm_animation.h"


/*
// アニメーションの管理、作成するクラス（ シングルトン ）
class AnimationFactory {
private:
	AnimationFactory();
	~AnimationFactory();

	// アニメーションを保存するコンテナ
	std::unordered_map < std::string, Animation >
		anim_container_;

public:
	static AnimationFactory* GetInstance();
	static void Destroy();

};
*/